phrase = "Hello, world!"

first_symbol = phrase[0]  # 'H'
third_symbol = phrase[2]  # 'l'

length = len(phrase)  # 13

last_symbol = phrase[-1]  # '!'

print(f"First symbol: {first_symbol}")
print(f"Length: {length}")
print(f"Last symbol: {last_symbol}")
